{
  "version": 2,
  "buildCommand": "cd frontend && npm install && npm run build && cp -r frontend/dist/* .",
  "outputDirectory": "dist",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}
